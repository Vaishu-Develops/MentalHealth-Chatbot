services:
  - type: web
    name: mindful-companion
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python start_combined_server.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: TEST_MODE
        value: false
    healthCheckPath: /health
